# Java
build/
*.class
*.jar
*.war

# Gradle
.gradle/
!backend/gradle/wrapper/gradle-wrapper.jar

# Generated
**/src/main/generated/

# Gradle build output
**/build/

# Python
__pycache__/
*.pyc
venv/

# Environment
.env
*.env.local
.env.prod

# 스코어링 상세 설계 (공개 저장소 노출 방지, 로컬 전용)
quant-engine/docs/SCORING_DESIGN.md

# 스코어링/백테스팅 방법론 외부 리뷰(claude-fable-5) 전문 보존, 로컬 전용
quant-engine/docs/BACKTEST_METHODOLOGY_REVIEW.md

# 포트폴리오 문서 3종 (공개 저장소 노출 방지, 로컬 전용) - QuantLime·HandsUp
# 2026-08-15 재편: PORTFOLIO_HIGHLIGHTS.md를 없애고, 이번에 최종 채택된
# 항목의 상세 원문을 PORTFOLIO_NOTES.md로 병합 + 미채택 원재료를
# PORTFOLIO_BACKLOG.md로 분리했다(이전엔 원재료 파일 1개에 전체가 섞여
# 있었으나, 채택 여부로 파일 자체를 나눠 제출본 범위를 명확히 함)
docs/00 Porfolio/PORTFOLIO_NOTES.md
docs/00 Porfolio/PORTFOLIO_BACKLOG.md
# 위 자료를 선별·요약한 제출용 큐레이션본 (2026-08-05 신규, 동일 사유로 로컬 전용)
docs/00 Porfolio/PORTFOLIO.md
# 큐레이션본을 Obsidian(docs/를 볼트로 사용)으로 열 때 생기는 볼트 설정/플러그인
# 캐시 - 포트폴리오 내용과 무관하지만 같은 폴더 하위라 실수로 커밋되지 않게 제외
docs/.obsidian/
# 프로젝트 루트 자체를 Obsidian 볼트로도 열 때 생기는 동일한 개인 설정 캐시
.obsidian/

# 세션 작업 문서(내부 컨벤션·변경 이력·로드맵·운영 런북) - public 저장소
# 노출 방지 목적으로 로컬 전용 전환(2026-08-03, git-filter-repo로 과거
# 히스토리에서도 제거). 파일 자체는 로컬에 계속 유지하고 git 추적만 제외
CLAUDE.md
backend/CLAUDE.md
frontend/CLAUDE.md
docs/03 Resource/CHANGELOG.md
# 2026-08-19: CHANGELOG.md가 계속 커지는 문제로 지난 달치를 분리한 아카이브
docs/03 Resource/CHANGELOG_ARCHIVE.md
docs/03 Resource/ROADMAP.md
# 이력서/포트폴리오(캔바) 개편 진행 상황 추적 - 프로젝트 로드맵과 무관한
# 개인 커리어 문서라 별도 파일로 분리, 동일 사유로 로컬 전용(2026-08-09)
docs/00 Porfolio/RESUME_ROADMAP.md
docs/03 Resource/DEVELOPMENT.md
docs/03 Resource/DEPLOYMENT.md
# 장애 허용성 감사 결과(SPOF 목록·인프라 세부사항 포함) - 위와 동일한
# 사유로 로컬 전용(2026-08-17)
docs/02 Tech/RELIABILITY.md
# 성능 개선 스코어카드(병목 실측치·인프라 세부사항 포함) - 위와 동일한
# 사유로 로컬 전용(2026-08-20)
docs/02 Tech/PERFORMANCE.md
# 부하테스트 가이드(위험 경로 목록·인프라 세부사항 포함) - 위와 동일한
# 사유로 로컬 전용 전환. 과거 커밋(fb571fc)엔 남겨두고 추적만 제외
# (2026-08-20)
docs/02 Tech/LOAD_TESTING.md
# 채용공고 정리 - RESUME_ROADMAP.md와 동일한 사유(개인 구직 문서)로
# 로컬 전용(2026-08-20). 노트 하나=공고 하나 구조(Bases)로 전환.
# 2026-08-21: job-postings/ 하위 폴더 없이 개별 공고 노트가 이 폴더에
# 직접 들어가는 구조로 바뀌어 폴더 전체를 제외 대상으로 통일
docs/01 Job-Postings/

# 사용자 업로드 이미지(로컬 디스크 저장, FileStorageService 참고) - 실행 시
# 자동 생성되는 런타임 데이터라 저장소에 커밋하지 않는다. 실행 위치(루트
# vs backend/api)에 따라 상대경로 UPLOAD_DIR이 둘 중 한 곳에 생길 수 있음.
backend/uploads/
backend/api/uploads/

# JVM 힙 덤프(OOM 분석 등으로 로컬에 우발적으로 생성됨, 수백MB~ 크기라
# 저장소에 절대 커밋하지 않는다)
*.hprof

# Playwright MCP 임시 아티팩트
.playwright-mcp/

# 로컬 개발용 파일 로그(application.yml dev 프로파일, logging.file.name) -
# 실행 위치(루트/backend/backend/api)에 따라 상대경로가 셋 중 한 곳에 생길
# 수 있음(backend/uploads/ 케이스와 동일 사유)
logs/
backend/logs/
backend/api/logs/

# node-exporter textfile collector - scripts/backup-mysql.sh가 EC2
# 호스트에서 크론으로 실행되며 기록하는 런타임 메트릭 파일(2026-08-17,
# MysqlBackupStale 알림). 디렉터리 자체는 .gitkeep으로 유지.
monitoring/textfile-collector/*.prom

# k6 부하테스트 산출물/시크릿 - 결과 파일은 런마다 수십~수백MB(json.gz
# 원본 포함), tokens.json은 오프라인 서명한 실제 JWT라 커밋 금지
# (2026-08-17, load-test/README.md 참고)
load-test/results/
load-test/data/tokens.json
load-test/config/env

# Node
node_modules/

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
